spec: rms/contract/v0.1
name: "run-semantic-properties"
version: 1
kind: command
meaning: "Public command `rms-property-run` declared by RMS semantic change."
preconditions:
  - id: "accepts-1"
    statement: "implementation target with commands.properties or commands.fuzz"
postconditions:
  - id: explicit-outcome
    statement: "The command returns an explicit accepted or rejected outcome."
failure_categories:
  - id: "rejects-1"
    statement: "missing declared property or fuzz verification command"
compatibility:
  policy: backward-compatible-within-major
