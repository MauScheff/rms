# Contract Evidence: refactor-module-workbench

Covered by `cargo test --manifest-path Cargo.toml`, including deterministic prompt-rendering coverage for `rms.refactor@v1`. The command is advisory by default, asks for behavior-preserving internal changes, and can write a run record or execute through an optional provider using the same prompt.
