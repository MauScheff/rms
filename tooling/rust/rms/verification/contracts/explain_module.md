# Contract Evidence: explain-module

Covered by `cargo test --manifest-path Cargo.toml` and CLI execution against fixture modules. The command renders module purpose, ownership, public surface, invariants, effects, compatibility, verification evidence, and useful follow-up commands from the target manifest. Tests cover inferred-module explanation and provider-ready prompt rendering through the shared workbench prompt path.
