# Contract Evidence: add-rms-capability-tree

Promise: the command creates an explicit composite, decision child, and boundary child from supplied topology parameters, and only creates a runnable surface when `--surface` is supplied.

Command/tool:

- `cargo test -p rms add_capability_`
- `cargo test -p rms removed_add_capability_command_is_unavailable`

Expected result: recursive composition verifies, neutral child defaults remain stable, browser assets appear only for explicit browser surface input, and the old ambiguous command does not parse.

Source revision: strict audit binds this evidence to the committed candidate.
