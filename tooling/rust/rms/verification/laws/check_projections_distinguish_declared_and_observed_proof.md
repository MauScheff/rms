# Law evidence: check projections distinguish declaration from observation

Proof projection is constructed from commands and validators RMS actually ran. Canonical property realizations and trace producers remain under `declared_not_observed` unless an execution record identifies the exact subject, class, strategy, and profile.

Verification:

- `cargo test --manifest-path tooling/rust/rms/Cargo.toml check_proof_projection_never_promotes_declarations`
