# Law Evidence: property evidence obligations are not proof

Promise:

- Law `property-evidence-obligation-is-not-proof` requires untouched property evidence generated by `rms spec apply` to remain production-blocking.

Scenario:

- Apply a semantic property with an evidence path and inspect the generated file before any observed result is recorded.
- Replace the obligation with concrete command and result evidence and validate it again.

Command/tool:

- `cargo test --manifest-path tooling/rust/rms/Cargo.toml spec_apply_change_yaml_adds_semantic_property_and_evidence`
- `cargo run --manifest-path tooling/rust/rms/Cargo.toml -- property check tooling/rust/rms/module.yaml --strict`

Observed result:

- The regression test passed and asserted `evidence.placeholder` for the untouched generated obligation.
- The strict property check accepts this file after the observed command and result replace the obligation text.

Source revision: resolved and recorded by `rms audit --strict` for the committed release candidate.
