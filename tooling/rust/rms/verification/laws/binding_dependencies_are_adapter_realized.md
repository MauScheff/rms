# Law Evidence: binding dependencies are adapter-realized

Promise: canonical module dependency semantics are translated by binding adapters into idiomatic local dependency declarations without becoming a second semantic authority.

Scenario: a recursive Rust capability tree records the domain provider in the boundary implementation allowlist and writes the matching sibling Cargo path dependency. A Python semantic apply records the provider module and its normalized import package, then realizes every declared Python command with an RMS-owned sibling `src` import path. Removing the dependency restores the original commands.

Command/tool:

- `cargo test -p rms add_capability_realizes_same_binding_local_dependency`
- `cargo test -p rms spec_apply_realizes_language_neutral_binding_dependencies`
- `cargo test -p rms spec_apply_realizes_python_binding_dependencies`

Expected result: canonical dependency identity and binding-native execution remain aligned for Rust and Python semantic changes; the Python consumer imports its sibling package without adopter-authored path wiring.

Source revision: strict audit binds this evidence to the committed candidate.
