spec: rms/contract/v0.2
name: checkout-completed
version: 1
kind: event
meaning: Published fact that checkout reached an accepted completed outcome.
semantics:
  event:
    observations:
    - id: event-emitted
      source:
        kind: input
        input_kind: event
        name: checkout-completed
      value: occurrence
    guarantees:
    - id: typed-event-payload
      statement: The event is emitted as its declared closed event variant.
      evaluation:
        kind: core
        expression:
          occurred: event-emitted
