spec: rms/contract/v0.2
name: order-ready-for-fulfillment
version: 1
kind: event
meaning: Ordering context fact that fulfillment may proceed.
semantics:
  event:
    observations:
    - id: event-emitted
      source:
        kind: input
        input_kind: event
        name: order-ready-for-fulfillment
      value: occurrence
    guarantees:
    - id: typed-event-payload
      statement: The event is emitted as its declared closed event variant.
      evaluation:
        kind: core
        expression:
          occurred: event-emitted
