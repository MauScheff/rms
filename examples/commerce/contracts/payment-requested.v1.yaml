spec: rms/contract/v0.2
name: payment-requested
version: 1
kind: event
meaning: Ordering context fact requesting payment coordination.
semantics:
  event:
    observations:
    - id: event-emitted
      source:
        kind: input
        input_kind: event
        name: payment-requested
      value: occurrence
    guarantees:
    - id: typed-event-payload
      statement: The event is emitted as its declared closed event variant.
      evaluation:
        kind: core
        expression:
          occurred: event-emitted
