[env]
# Clap's complete RMS command tree exceeds Rust's 2 MiB Linux test-thread
# default in debug builds. Keep parser/help tests representative and portable.
RUST_MIN_STACK = { value = "8388608", force = false }
